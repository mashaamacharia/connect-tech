# WordPress GraphQL API Configuration
# Replace with your actual WordPress GraphQL endpoint URL
NEXT_PUBLIC_WORDPRESS_GRAPHQL_URL=http://localhost/PHPproject/wp_headless/graphql

# Alternative: Use WORDPRESS_GRAPHQL_URL for server-side only (if needed)
# WORDPRESS_GRAPHQL_URL=http://localhost/PHPproject/wp_headless/graphql

# Optional: WordPress Authentication Token (if your WordPress site requires authentication)
# WORDPRESS_AUTH_TOKEN=your_auth_token_here